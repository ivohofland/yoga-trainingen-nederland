This is a fixture standing in for the authoring README that lives in
content/notities/. It deliberately has no frontmatter block: if the loader
ever stopped skipping README.md, parsing it as a post would throw loudly here
instead of passing silently.
